#
# cumulative environment (on top of fedbiomed) for fedbiomed-researcher
#
#
#
name: fedbiomed-researcher

channels:
  - conda-forge

dependencies:
  - python
  - pip
  - pytest
  - jupyter
  - tensorboard
  - requests
  - paho-mqtt
  - pandas
  - validators
  - pip:
      - torch
      - persist-queue
