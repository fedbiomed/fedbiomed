#
# environment for fedbiomed-network
#
#
#
name: fedbiomed-network

channels:
  - conda-forge

dependencies:
  # common
  - python >=3.9,<4.0
  - pip
  # http server
  - django = 3.1.7
  - djangorestframework = 3.12.2
  - django-cleanup
  - gunicorn
  # for utilities
  - pip:
      - persist-queue
