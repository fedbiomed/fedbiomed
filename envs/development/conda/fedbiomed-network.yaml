#
# environment for fedbiomed-network
#
#
#
name: fedbiomed-network

channels:
  - conda-forge

dependencies:
  # common
  - python
  - pip
  # http server
  - django
  - djangorestframework
  - django-cleanup
  - gunicorn
  # for utilities
  - pip:
      - persist-queue
