#!/bin/bash

# add commands to be run before notebook startup

#COMMAND="PATH=$PATH conda install -y pymongo=3.10.1"
#COMMAND_CONTEXT="bash -c "
#[ -n "NB_USER" ] && COMMAND_CONTEXT="/bin/su - $NB_USER -c "
#$COMMAND_CONTEXT "$COMMAND"
